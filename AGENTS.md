The goal of this project is to make a CLI based website that the user SSH's into.

  The project is rust based using crossterm and ratatui. Check cargo.toml for the versions
i want the theme of the tui to look like claude code. this is databaseless products are hardcoded in.

